# Manual / local entry: use repo-root `.github/workflows/newdb-ci.yml` for PR/push/schedule.
name: newdb-ci

on:
  workflow_dispatch:

jobs:
  reusable-ci:
    uses: ./newdb/.github/workflows/newdb-ci-reusable.yml
