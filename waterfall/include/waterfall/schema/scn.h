#pragma once

// Canonical include path for `waterfall/schema/scn.h` without relying on the repo-root include.
// Legacy TU may still resolve via `${CMAKE_CURRENT_SOURCE_DIR}/..` on the `waterfall` target.
#include "../../../schema/scn.h"
