# Manual / local entry: use repo-root `.github/workflows/newdb-ci.yml` for PR/push/schedule.
# Reusable jobs live in `.github/workflows/newdb-ci-reusable.yml` (repository root; required by GitHub).
name: newdb-ci

on:
  workflow_dispatch:

jobs:
  reusable-ci:
    uses: ./.github/workflows/newdb-ci-reusable.yml
